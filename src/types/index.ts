export interface ProjectProps {
  index: number;
  title: string;
  description: string;
}

export interface BlogProps {
  index: number;
  title: string;
  description: string;
}

export interface ReviewProps {
  index: number;
}
